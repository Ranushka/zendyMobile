class UserModel {
  String firstName, lastName, email, id, phoneNumber, authToken, theme;

  UserModel({
    this.firstName,
    this.lastName,
    this.email,
    this.id,
    this.phoneNumber,
    this.authToken,
    this.theme,
  });
}
